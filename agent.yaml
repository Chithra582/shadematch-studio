spec_version: "0.1.0"
name: shadematch-studio
version: 1.0.0
description: >
  Client-side beauty discovery studio. Matches a user's skin tone to
  compatible foundation/concealer shades from a curated catalog and
  surfaces matching looks and artists.
skills:
  - shade-matching
tools:
  - name: match_shades
    description: Client-side function that ranks catalog shades against a user-selected or detected skin tone value